{
  "name": "new-cypress-framework",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run-reports": "npx cypress run --record false --browser chrome && npm run generate:mochawesome-report",
    "generate:mochawesome-report": "npx mochawesome-merge cypress/results/mochawesome/*.json > cypress/results/mochawesome/combined-report.json && npx marge cypress/results/mochawesome/combined-report.json -o cypress/results/mochawesome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.4.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
